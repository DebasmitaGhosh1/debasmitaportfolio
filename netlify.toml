[build]
command = "./gradlew kobwebExport"  # Command to build your site using Gradle and Kobweb
publish = "site/build/dist/jsProductionExecutable"  # Path where the built output will be

[build.environment]
JAVA_VERSION = "11"  # Ensures that Java 11 is used during the build process
